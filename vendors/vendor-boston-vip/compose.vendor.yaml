services:
  limo-cell-boston-vip:
    build:
      context: ../..
      dockerfile: Dockerfile
    container_name: limo-cell-boston-vip
    restart: always
    ports:
      - "8003:8000"
    env_file:
      - ../../.env
      - ./vendor.env
    environment:
      - MYSQL_HOST=limo-mysql
      - MYSQL_PORT=3306
      - GLOBAL_HUB_URL=http://limo-global-hub:8000
    depends_on:
      limo-mysql:
        condition: service_healthy
    networks:
      - limo-net
